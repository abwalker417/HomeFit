# HomeFit Git Notes

Last updated: 2026-08-14 MDT

The authoritative handoff is `README-homefit-notes.md`. Read it with
`AGENTS.md` before changing routes, workout generation, deployment, or the V1
migration workflow.

## Branch and deployment

- Active V2 branch: `codex/v2-foundation`
- V2 server: CT120 at `192.168.68.20:5000`
- Update: `pct exec 120 -- /usr/local/sbin/homefit-update`
- V1 CT115 is a read-only migration source; never share its writable SQLite DB.

## Guardrails

- Do not add workout focus back to onboarding.
- Keep navigation at the top and Coach/APEX as a normal tab.
- Preserve focus/equipment/limitation validation in both local and AI workouts.
- Do not commit secrets, SQLite runtime files, or `*.hekate-bak` artifacts.
- Do not expose the garage panel publicly without an explicit access-control
  decision.
